{
  "name": "ms-booking",
  "version": "0.0.0",
  "private": true,
  "description": "Description for ms_booking",
  "license": "UNLICENSED",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "app:up": "docker compose -f docker/app.yml up --wait",
    "build": "npm run build -w server && npm run build -w client",
    "build:app": "npm run build -w server",
    "ci:e2e:prepare": "npm run ci:e2e:prepare:docker",
    "ci:e2e:prepare:docker": "npm run services:up --if-present && docker ps -a",
    "ci:e2e:teardown": "npm run ci:e2e:teardown:docker --if-present",
    "ci:e2e:teardown:docker": "docker compose -f docker/services.yml down -v && docker ps -a",
    "docker:consul:down": "docker compose -f docker/consul.yml down -v",
    "docker:consul:up": "docker compose -f docker/consul.yml up --wait",
    "docker:db:down": "docker compose -f docker/mysql.yml down -v",
    "docker:db:up": "docker compose -f docker/mysql.yml up --wait",
    "docker:keycloak:down": "docker compose -f docker/keycloak.yml down -v",
    "docker:keycloak:up": "docker compose -f docker/keycloak.yml up --wait",
    "docker:redis:down": "docker compose -f docker/redis.yml down -v",
    "docker:redis:up": "docker compose -f docker/redis.yml up --wait",
    "lint:fix": "echo \"INFO: no client found\"",
    "prettier:check": "prettier --check \"{,src/**/}*.{md,json,yml,js,cjs,mjs,ts,cts,mts}\"",
    "prettier:format": "prettier --write \"{,src/**/}*.{md,json,yml,js,cjs,mjs,ts,cts,mts}\"",
    "services:up": "docker compose -f docker/services.yml up --wait",
    "start": "concurrently -k -s first -n application,client -c red,green npm:start:server npm:start:client",
    "start:client": "npm run start -w client",
    "start:server": "npm run start -w server",
    "test": "echo \"INFO: no client test found\"",
    "test:client": "npm test -w client",
    "test:server": "npm test -w server",
    "test:server:e2e": "npm run test:e2e -w server"
  },
  "config": {
    "default_environment": "prod"
  },
  "devDependencies": {
    "@eslint/js": "9.23.0",
    "browser-sync-client": "3.0.4",
    "eslint": "9.14.0",
    "eslint-config-prettier": "10.1.1",
    "generator-jhipster": "8.10.0",
    "generator-jhipster-nodejs": "3.2.0",
    "globals": "16.0.0",
    "prettier": "3.5.3",
    "prettier-plugin-packagejson": "2.5.10",
    "typescript-eslint": "8.29.0"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "cacheDirectories": [
    "node_modules"
  ]
}
